// Tailwind v4 sits alongside the hand-written design system.
// Preflight is deliberately not imported — globals.css owns the reset.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
